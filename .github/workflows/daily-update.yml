name: 每日数据更新

on:
  schedule:
    # 北京时间每天早上 7:00 运行
    - cron: '0 23 * * *'
  workflow_dispatch:  # 允许手动触发

jobs:
  update-data:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: 安装依赖
        run: pip install requests beautifulsoup4

      - name: 注入 DeepSeek API Key 到前端
        env:
          DEEPSEEK_API_KEY: ${{ secrets.DEEPSEEK_API_KEY }}
        run: |
          sed -i "s|const DEEPSEEK_KEY='[^']*'|const DEEPSEEK_KEY='${DEEPSEEK_API_KEY}'|g" index.html

      - name: 抓取数据
        env:
          DEEPSEEK_API_KEY: ${{ secrets.DEEPSEEK_API_KEY }}
        run: python .github/scripts/fetch_hot.py

      - name: 提交更新
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add data/hot.json data/weekly_menu.json data/weekly_exercise.json data/daily_read.json data/daily_words.json index.html
          if git diff --staged --quiet; then
            echo "No changes to commit"
          else
            git commit -m "🤖 每日数据自动更新 $(date +%Y-%m-%d)"
            git push
          fi
